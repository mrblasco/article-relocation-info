library(here)

config <- list(
    data_dir = here("data", "processed"),
    output_figures = here("outputs", "figures"),
    output_models = here("outputs", "models"),
    seed = 1234,
    cores = 4
)
